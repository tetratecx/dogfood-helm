apiVersion: v1
kind: Service
metadata:
  name: {{ include "dogfood-cert-manager.fullname" . }}
  labels:
    {{- include "dogfood-cert-manager.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dogfood-cert-manager.selectorLabels" . | nindent 4 }}
