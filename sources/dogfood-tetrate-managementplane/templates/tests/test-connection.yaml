apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dogfood-tetrate-managementplane.fullname" . }}-test-connection"
  labels:
    {{- include "dogfood-tetrate-managementplane.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dogfood-tetrate-managementplane.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
