apiVersion: v1
kind: Service
metadata:
  name: {{ include "dogfood-tetrate-managementplane.fullname" . }}
  labels:
    {{- include "dogfood-tetrate-managementplane.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dogfood-tetrate-managementplane.selectorLabels" . | nindent 4 }}
