# Available NGAC CRDs. Documentation only.
# These are going to be managed by the TSB Management Plane operator. They should not be installed by Helm.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

# DO NOT EDIT - Generated by Cue OpenAPI generator based on Istio APIs.
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: accesspolicies.policy.ngac.tetrate.io
spec:
  group: policy.ngac.tetrate.io
  names:
    categories:
    - tetrate-io
    - ngac-tetrate-io
    - policy-ngac-tetrate-io
    kind: AccessPolicy
    listKind: AccessPolicyList
    plural: accesspolicies
    shortNames:
    - nap
    singular: accesspolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: The PolicyClass the access policy belongs to
      jsonPath: .spec.policyClass
      name: Policy Class
      type: string
    - description: Target attribute the policy is applied on
      jsonPath: .spec.targetAttribute
      name: Target
      type: string
    - description: Permissions granted to the sources
      jsonPath: .spec.permissions
      name: Permissions
      type: string
    - description: Details about the synchronization of this object to the graph.
        Completed means the PolicyClass is ready to be used.
      jsonPath: .status.conditions[?(@.type contains "GraphSync")].status
      name: Graph Sync
      type: string
    - description: 'CreationTimestamp is a timestamp representing the server time
        when this object was created. It is not guaranteed to be set in happens-before
        order across separate operations. Clients may not set this value. It is represented
        in RFC3339 form and is in UTC. Populated by the system. Read-only. Null for
        lists. More info: https://git.k8s.io/community/contributors/devel/api-conventions.md#metadata'
      jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        properties:
          spec:
            properties:
              id:
                type: string
              permissions:
                items:
                  type: string
                type: array
              policyClass:
                type: string
              sources:
                items:
                  properties:
                    kind:
                      description: The kind of the resource.
                      type: string
                    name:
                      description: The name of the resource.
                      type: string
                  type: object
                type: array
              targetAttribute:
                type: string
            type: object
          status:
            type: object
            x-kubernetes-preserve-unknown-fields: true
        type: object
    served: true
    storage: true
    subresources:
      status: {}

---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: attributeassignments.policy.ngac.tetrate.io
spec:
  group: policy.ngac.tetrate.io
  names:
    categories:
    - tetrate-io
    - ngac-tetrate-io
    - policy-ngac-tetrate-io
    kind: AttributeAssignment
    listKind: AttributeAssignmentList
    plural: attributeassignments
    shortNames:
    - naa
    singular: attributeassignment
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: The PolicyClass the attribute belongs to
      jsonPath: .spec.policyClass
      name: Policy Class
      type: string
    - description: Details about the synchronization of this object to the graph.
        Completed means the PolicyClass is ready to be used.
      jsonPath: .status.conditions[?(@.type contains "GraphSync")].status
      name: Graph Sync
      type: string
    - description: 'CreationTimestamp is a timestamp representing the server time
        when this object was created. It is not guaranteed to be set in happens-before
        order across separate operations. Clients may not set this value. It is represented
        in RFC3339 form and is in UTC. Populated by the system. Read-only. Null for
        lists. More info: https://git.k8s.io/community/contributors/devel/api-conventions.md#metadata'
      jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        properties:
          spec:
            description: AttributeAssignments define Attributes and the resources
              that have those attributes.
            properties:
              id:
                type: string
              policyClass:
                type: string
              resources:
                description: List of resources that will be assigned to this attribute.
                items:
                  properties:
                    kind:
                      description: The kind of the resource.
                      type: string
                    name:
                      description: The name of the resource.
                      type: string
                  type: object
                type: array
              subjects:
                items:
                  properties:
                    kind:
                      description: The kind of the resource.
                      type: string
                    name:
                      description: The name of the resource.
                      type: string
                  type: object
                type: array
            type: object
          status:
            type: object
            x-kubernetes-preserve-unknown-fields: true
        type: object
    served: true
    storage: true
    subresources:
      status: {}

---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: policyclasses.policy.ngac.tetrate.io
spec:
  group: policy.ngac.tetrate.io
  names:
    categories:
    - tetrate-io
    - ngac-tetrate-io
    - policy-ngac-tetrate-io
    kind: PolicyClass
    listKind: PolicyClassList
    plural: policyclasses
    shortNames:
    - npc
    singular: policyclass
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Details about the synchronization of this object to the graph.
        Completed means the PolicyClass is ready to be used.
      jsonPath: .status.conditions[?(@.type contains "GraphSync")].status
      name: Graph Sync
      type: string
    - description: 'CreationTimestamp is a timestamp representing the server time
        when this object was created. It is not guaranteed to be set in happens-before
        order across separate operations. Clients may not set this value. It is represented
        in RFC3339 form and is in UTC. Populated by the system. Read-only. Null for
        lists. More info: https://git.k8s.io/community/contributors/devel/api-conventions.md#metadata'
      jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        properties:
          spec:
            properties:
              id:
                type: string
            type: object
          status:
            type: object
            x-kubernetes-preserve-unknown-fields: true
        type: object
    served: true
    storage: true
    subresources:
      status: {}

---
